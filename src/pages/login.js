import React from "react";
import { LoginFormContainer, MainLayout } from "layouts";

const Login = () => {
  return (
    <MainLayout>
      <LoginFormContainer />
    </MainLayout>
  );
};

export default Login;
