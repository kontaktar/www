import React from "react";

import { MainLayout, LoginFormContainer } from "layouts";

const Login = () => {
  return (
    <MainLayout>
      <LoginFormContainer />
    </MainLayout>
  );
};

export default Login;
