import React from "react";
import { MainLayout, RegisterContainer } from "layouts";

const Register = () => {
  return (
    <MainLayout>
      <RegisterContainer />
    </MainLayout>
  );
};

export default Register;
