export type UserSessionStorage = {
  login: string;
  isLoggedIn: boolean;
  id: string;
};

export type Card = {
  title: string;
  description: string;
  years: string;
  months: string;
  userName: string;
  experienceId: string;
};
