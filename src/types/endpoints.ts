// eslint-disable-next-line import/prefer-default-export
export enum Endpoint {
  Login = "/api/login",
  Logout = "/api/logout",
  Users = "/api/users",
  User = "/api/user",
  Search = "/api/search",
  Admins = "/api/admins"
}
